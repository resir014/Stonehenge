version: 2
jobs:
  build:
    working_directory: ~/Stonehenge
    docker:
      - image: circleci/node:6
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "package.json" }}
      - run:
          name: install-npm
          command: npm install
      - save_cache:
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - .node_modules
      - run:
          name: copy-configs
          command: cp src/config/config.ts.example src/config/config.ts
      - run:
          name: copy-job-listings
          command: cp src/config/jobs.ts.example src/config/jobs.ts
      - run:
          name: test
          command: npm test
      - store_artifacts:
          path: test-results.xml
          prefix: tests
      - store_artifacts:
          path: coverage
          prefix: coverage
      - store_test_results:
          path: test-results.xml
